export const APP_CONSTANTS = {
  GLOBAL_PREFIX: 'api',
  DEFAULT_PORT: 3000,
} as const;

export const CLOUDINARY_CONSTANTS = {
  UPLOAD_FOLDER: 'cooking-app/recipes',
  RESOURCE_TYPE: 'image',
} as const;
